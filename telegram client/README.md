## telegram bot
# 📌 Описание
Этот бот предназначен для взаимодействия пользователей с системой через Telegram. Он обеспечивает авторизацию через внешние сервисы (например, GitHub или Яндекс), хранит состояние пользователя в Redis и передаёт идентификатор пользователя в запросы к логическому модулю. Таким образом, бот связывает Telegram‑пользователя с внутренними сервисами и позволяет выполнять действия над данными системы прямо из мессенджера.

# ⚙️ Основные возможности
Инициация процесса авторизации через внешний модуль.

Сохранение временного состояния (state) и проверка результата авторизации.

Хранение идентификатора пользователя (user_id) в Redis по ключу user:{chat_id}.

Передача current_id в запросы к логическому модулю для выполнения операций.

Возможность выхода из системы и повторного входа.

Обработка ошибок Redis и HTTP‑запросов с информированием пользователя.

# 🛠️ Технические детали
Язык: Go

Хранение состояния: Redis

Авторизация: внешний модуль (GitHub, Яндекс)

Логический модуль: REST API, запросы вида:

Код
http://<LOGIC_SERVICE_URL>/api/users?current_id=<user_id>
Конфигурация задаётся через .env файл (токен бота, адрес Redis, URL сервисов).

# 🚀 Запуск
Установите зависимости:

bash
go mod tidy
Заполните файл .env:

env
TG_BOT_TOKEN=<ваш_токен>
REDIS_ADDR=localhost:6379
AUTH_SERVICE_URL=http://localhost:8080
LOGIC_SERVICE_URL=http://localhost:3415
GITHUB_CLIENT_ID=<client_id>
YANDEX_CLIENT_ID=<client_id>
Запустите Redis и логический модуль.

Запустите бота:

bash
go run main.go
📖 Логика работы
При первом обращении бот проверяет наличие пользователя в Redis.

Если пользователь неизвестен — предлагает авторизацию.

После успешного входа сохраняет user_id и использует его для всех запросов.

Все действия выполняются через логический модуль с передачей current_id.

# ✅ Вывод
Бот полностью реализует авторизацию, хранение состояния и интеграцию с логическим модулем. Он обеспечивает удобный интерфейс для работы с системой через Telegram, используя Redis для управления пользователями и их статусами.

